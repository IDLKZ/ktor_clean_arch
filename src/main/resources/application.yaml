ktor:
  application:
    modules:
      - kz.idl.ApplicationKt.module
  deployment:
    environment: dev
    port: 8080
    host: 127.0.0.1

# Общие настройки БД
database:
  jdbcUrl: "jdbc:postgresql://localhost:5432/ktor_back"
  username: "postgres"
  password: "root"
  driverClassName: "org.postgresql.Driver"

  # Pool размеры
  maximumPoolSize: 10
  minimumIdle: 2

  # Таймауты (в миллисекундах)
  idleTimeout: 600000        # 10 минут
  connectionTimeout: 30000   # 30 секунд
  maxLifetime: 1800000       # 30 минут
  validationTimeout: 5000    # 5 секунд

  # Дополнительно
  poolName: "MainHikariPool"
  autoCommit: true
  connectionTestQuery: "SELECT 1"

# Flyway использует те же настройки
flyway:
  enabled: true
  locations: "classpath:db/migration"
  baselineOnMigrate: true
  validateOnMigrate: true

# Файловый сервис
storage:
  uploadDir: "uploads"
  maxFileSizeMb: 10
  allowedExtensions:
    - jpg
    - jpeg
    - png
    - gif
    - pdf
    - doc
    - docx
    - xls
    - xlsx
  forbiddenExtensions:
    - exe
    - bat
    - sh
    - cmd
    - ps1
    - jar